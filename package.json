{
  "name": "openfront-utility-ts",
  "version": "1.0.0",
  "type": "module",
  "description": "Pattern JSON Generator + Preview ",
  "scripts": {
    "build": "tsc && node scripts/copy-static.js",
    "start": "npx serve docs",
    "dev": "npm run build && npm run start"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}